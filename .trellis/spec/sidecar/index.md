# Sidecar Specs

Conventions and hard contracts for the Python sidecar (`sidecar/`).

## Files

- [mcp-rpc-cli.md](mcp-rpc-cli.md) — Hard contracts for the three sidecar entry modes (Tauri stdio, CLI+daemon, MCP). Covers `@rpc_method` requirements, MCP tool naming/schema/error protocol, CLI `--json` discipline, and forbidden imports.
